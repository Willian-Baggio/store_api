CREATE TABLE addres (
  id INTEGER PRIMARY KEY AUTO_INCREMENT,
  street VARCHAR(50),
  neighborhood VARCHAR(50),
  zip_code VARCHAR(20),
  number INTEGER,
  complement VARCHAR(50),
  city VARCHAR(50),
  uf VARCHAR(2)
);